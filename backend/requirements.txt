Django>=4.2
djangorestframework>=3.14
django-cors-headers>=4.0
PyMySQL>=1.1
pytesseract>=0.3.10
Pillow>=10.0
Jinja2>=3.1
django-fsm>=2.8
reportlab>=4.0
djangorestframework-simplejwt>=5.3
gunicorn>=21.2
# ===== ASGI 服务器（SSE 流式端点必需，2026-07-07 新增）=====
uvicorn[standard]>=0.30
whitenoise>=6.6
# ===== LangChain + LangGraph（A6 新增）=====
langchain>=1.0,<2.0
langchain-core>=1.0,<2.0
langgraph>=1.2,<2.0
langgraph-checkpoint-postgres>=2.0
langchain-openai>=1.0
langchain-postgres>=0.0.10
langsmith>=0.3.0
# ===== PostgreSQL Checkpointer 依赖（psycopg binary + pool）=====
# pool extra 提供 ConnectionPool，binary extra 提供预编译 C 扩展
psycopg[binary,pool]>=3.1
# ===== pgvector（v10 新增：RAG 知识库向量检索）=====
pgvector>=0.3.0
# ===== BM25 检索（v10 新增：法条关键词检索 + RRF 融合）=====
# rank_bm25 提供 BM25Okapi 算法实现；jieba 提供中文分词
# 缺失会导致 rag_service._bm25_search() 静默返回空列表（BM25 永远为 0）
rank_bm25>=0.2.2
jieba>=0.42.1
# ===== LLM 服务支撑 =====
httpx>=0.27
python-dotenv>=1.0
tenacity>=8.0
# ===== LangExtract（字段抽取，Google 出品，通过 OpenAI 兼容接口调用 Qwen3）=====
# [openai] 可选依赖：启用 OpenAI 兼容 provider（Qwen3 / GPT / DeepSeek 等通用）
langextract[openai]>=0.1.0
# ===== OCR 多引擎（本地 PaddleOCR 为可选重依赖，容器部署默认关闭）=====
paddleocr>=3.0.0
paddlepaddle>=3.0.0
